-- V6 não é mais necessário — quantidade_gramas é calculada dinamicamente
-- via query param no endpoint, não persiste no banco.
-- Script mantido vazio para não quebrar o histórico do Flyway.
SELECT 1;
